

import java.util.List;
import java.util.stream.Collectors;

    /*
    public class TestIml {

 class Clidder
{
	private final void flipper()
	{
		System.out.println("Clidder");
	}
}

public class Clidlet extends Clidder
{
	public final void flipper()
	{
		System.out.println("Clidlet");
	}
	public static void main(String[] args)
	{
		new Clidlet().flipper();
	}
}
name1.equals(name2);
str1.equals(str2)
// firstName , LastName, Department
   boolean equals(Object o)
   {
   if(this.getFirstName.equals(o.firstName) && this.getLatName.equals(o.lastName)
   {
   return true;

   }
   if()
   {
      return true;

   }
   if(this.getDepartmentName.equals(o.departmentName)
   {
      return true;
 }
return false;

     */

   // List<Product> sortedProductNew = products.stream().sorted((o1, o2)-> (int)(o1.getPrice() - o2.getPrice())).collect(Collectors.toList());
/*
     List<Product> sortedProduct = products.stream().sorted(new Comparator<Product>() {
            @Override
            public int compare(Product o1, Product o2) {
                int p1 = o1.getPrice();
                int p2 = o2.getPrice();
                return p2-p1;
            }
        }).collect(Collectors.toList());
        sortedProduct.forEach(System.out::println);




 */
 /*
stream().sorted(o1 ,o2 ->)
        int comapre(Object o1 , Object o2)
        {
            int a1  = o1.getAge();
            int a2 = o2.getAge();
            return a1 - a2;
        }
}.collect.Collectors(asList)

  */
///
//Second Highest salary
// select max(salary) from Employee where salary < Select Max(Salary) from Employee
// array {1,2,3,5,6,7,8,9,10,11};
//using stream

///